  l : level
  i, i' : [Times Bool (Option TInput)]
  H : rel (eqpair_R (semiprivateRel Bool) (eqmaybe_top (publicRel TInput))) l i i'
  v, v' : [Bool]
  H0 : rel (semiprivateRel Bool) l v v'
  ============================
  rel (semiprivateRel Bool) l (xor i.1 v) (xor i'.1 v')

It reduces to showing i.1 and i'.1 are related, ideally equal. But rel (eqpair_* _ _) always reduces to indistinguishable \/ unobservable.
a R a' \/ dis a /\ dis 'a. We have dis a -> dis a' -> a R a'.
But we don't have a R a' \/ dis a /\ dis 'a -> a R a'. Oh yes we do, that's not the issue.
It is rel (eqpair_* _ _) (a,b) (a',b') = a Ra a' /\ b Rb b' \/ dis b /\ dis b'.
We cannot obtain the pairwise equality without the unobservable disjunct.

Key part here is H
    rel (eqpair_R (semiprivateRel Bool) (eqmaybe_top (publicRel TInput))) l i i'

Options:
1) change eqpair_R, consider the intuition behind the (b,Some t) being public even though b is private. Does it make sense?
2) investigate eqmaybe_top (publicRel TInput) (i.2 = None, i'.2 = None) is an interesting case

We start with 2)
If we replace eqmaybe_top with publicRel, then unobservable disjunct always false. Solves it.
But since InputRel uses eqpair_L at the top level

    eqpair_L (eqmaybe_top (publicRel TInput)) InputRel2

input (None,None,None) becomes public and route_aux maps private tuples like (None,Some _,None) to (None,(None,None)) and it needs to preserve f_PU: unobservable preservation. These bad tuples are not possible to produce but if we want to use that fact, then unobservability cannot be defined compositionally, since (None,None,Some _) is possible, but (None,Some _,None) is not, and I think if we were to do this, we would take advantage by making the bad tuples public using non compositinoal unobservability.


so the problem is if we have scheduled low, and send in (None,None,None). When 
