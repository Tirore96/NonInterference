Made progress. The challenging switch case is actually not an issue for the two secret processes. Only for the public one.
Reason being, the aware \/ oblivous side condition
all processes are wrapped in sta, which introduces eqpair_R as their input relation. To apply swi_NI, this must be changed to eqpair_LR, forcing left relation to use max distigunshed level used in right component, which is distinguished for bot, aka semiprivateRel

We are then left with showing:

  forall l : level,
  aware (semiprivateRel Bool) true l \/
  oblivious (eqpair_R (semiprivateRel Bool) (publicRel TPublicOutput)) (maybe (map inl_some [eta pair false] (maybe (my_procs 2)))) l

If we go by cases, l <> \bot follows from semiprivateRel reducing to equality
Case l = \bot, we must go with oblivious disjunct. There are more issues, but as a small aside the interplay between outer sta_NI leading to eqpair_R -> eqpair_LR (semiprivateRel _) _, being used in swi_NI, seems limiting in how swi_NI can be used. This seems like a problem in sta_NI.

Let us now continue with our work around which I'm afriand only works because there is something wrong with the theory.

As Willard mentioned, distinguishability is not important for output relations only what we can relate. We therefore have NI_O_imp

Lemma NI_O_imp : forall I O (IRel : myrel [I]) (ORel ORel' : myrel [O]) R,
    (forall l x y, rel ORel l x y -> rel ORel' l x y) ->
    NI _ _ IRel ORel R ->
    NI _ _ IRel ORel' R.

Which allows swapping output relations without considering dis ORel and dis ORel'

We need our relation to be oblivious for \bot so we change outer eqpair_R to eqpair_L.

From: (eqpair_R (semiprivateRel Bool) (publicRel TPublicOutput))

To: (eqpair_L (semiprivateRel Bool) (publicRel TPublicOutput))


Now my concern.
Is this flexibility unsound. All relations satisfy dis x -> dis y -> rel x y. So if we apply NI_O_imp we don't want to reduce the elements we relate.
We can however not increase it.



I found the issue. Thes swi_NI theorem may be fine. It's because we input the sum type to swi before projecting. Collectively they have the higher security level even though we know that we later will project. We commute projection out in front of swi. This makes the input relation (when we arrive at swi) fully public, and this allows us to replace eqpair_R with eqpair_LR.

This does not work because we cannot project and then get a rel that is fully public. Mapping distingushed values to None will require a eqmaybe_top structure. There the _R to _LR trick does not work.


Interesting. The fact that we arrive at a proof state where we need eqpair_R boolrel _, is sus. It means that we may be varying the switch


Resolving the R vs _LR problem

It seems that the input type to the process pool needs to be a product rather than a sum. This avoids leaking information, since projecting discards info where as destructing a summation reveals something when mapping to None.
