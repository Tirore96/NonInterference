Notes from latest supervision meeting

Filter handler output to private process with maybe, should not receive nothing, only notify

Type of scheduler should be Proc DiskInterrupt (option {W,R}) where R and None are private so that they cannot be distinguished

We can look into a more general process pool definition as Proc Sum ... Prod .....
Two versions, non-interacting vs interacting. The interacting version would be Proc V V as a big type that projects the relevant output into the input again. Actually makes sense given the loop construct. I was considering a functino from type to type that would allow some of the types to coincide but maybe willards suggestion is better.

The scheduler should be more generic, both in order and duration.


